# Copy to .env (or set these in the shell / CI) to wire the frontend to the backend.

# Base URL of the InsightLoop API. Must end with "/".
# All endpoints are resolved against this (see src/api/endpoints.js).
VITE_API_URL=http://localhost:5000/api/
